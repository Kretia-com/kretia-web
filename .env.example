# --- Email opcional (rellena para recibir los leads por correo) ---
# Si dejas esto vacío, los leads se ven igualmente en los logs de Railway.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
# A dónde llegan los leads (por defecto, a SMTP_USER):
LEAD_TO=
LEAD_FROM=
